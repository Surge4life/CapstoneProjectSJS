# Holdings Divisions · Live Truth (Capstone)

**URL (after Core deploy):** https://gods-platform-core.onrender.com/divisions

## Rule

Show **API reality**, including **zeros**. Do not use GODS Admin static theatre numbers (1.41M decisions, 39 SETHS placed, 14 systems) as Capstone truth.

## Cards

| Division | Sources |
|----------|---------|
| SETHS | `/analytics/SETHS/kpis` + `/seths/metrics` |
| MADIBA | `/analytics/MADIBA/kpis` + `/madiba/metrics` |
| TS | `/analytics/TS/kpis` + `/ts/metrics` |
| UDOC | regulator summary + UDOC analytics + demo/ready |
| GIS/GBS | `/gis/gbs/pillars` + `/gis/gbs/nodes` |

## Note on GODS Admin HTML

`/admin` remains constitutional narrative UI with some static copy. **Operational truth for divisions** = `/divisions` + UDOC surfaces. Full rewrite of 185KB admin.html is deferred; honesty path is this live dashboard.

Login: `admin@gods.local` / `admin123`
